/**
 * Background service worker entry point.
 * Manages LSP request routing, worker pool, caching, and GitHub API integration.
 */

// Placeholder — implemented in Phase 2
console.debug('[gh-lsp] Background service worker loaded');
