/**
 * Content script entry point.
 * Detects supported GitHub code pages, initializes DOM observation,
 * token detection, and messaging with the background service worker.
 */

// Placeholder — implemented in Phase 5
console.debug('[gh-lsp] Content script loaded');
